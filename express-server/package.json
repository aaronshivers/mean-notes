{
  "name": "notes",
  "version": "1.0.0",
  "description": "An api for notes",
  "main": "src/server.js",
  "scripts": {
    "test": "cross-env NODE_ENV=test mocha --exit **/*.spec.js",
    "test-watch": "nodemon --exec 'npm test'",
    "start": "cross-env NODE_ENV=production node src/server.js",
    "dev": "cross-env NODE_ENV=development nodemon src/server.js"
  },
  "keywords": [],
  "author": "aaron shivers",
  "license": "GPL-3.0",
  "devDependencies": {
    "dotenv": "^8.2.0",
    "expect": "^25.1.0",
    "mocha": "^7.0.1",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.0",
    "express": "^4.17.1",
    "mongodb": "latest",
    "mongoose": "^5.9.1"
  }
}
